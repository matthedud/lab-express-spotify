<section>
<h1>tracks for: {{artist.name}}</h1>
  {{#each tracks}}
    <div class="tracks">
      <p>{{this.name}}</p>
      <audio controls src="{{this.preview_url}}"></audio>
    </div>
  {{/each}}
</section>